Build (client)/complete-profile.tsx, reachable via a lightweight prompt on the Client dashboard (optional, since Clients aren't gated by verification).

Capture any remaining detail not asked during Act 1:
- Precise location/address book (saved addresses)
- Full payment method setup (placeholder only, no real integration yet)

Store in store/useClientProfileStore.ts.